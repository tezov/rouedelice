#Common configuration
domain: com.tezov
sub_domain: rouedelice

#Dependencies
#dependencies catalog outside project, not in this git repo (check copy in you need it)
dependencies : ${file://../../catalog.dependencies.yaml}

#Modules configuration
plugins_apply_to:
  app_client_android:
    - ${plugins.application}
    - ${plugins.kotlin}
    - ${plugins.kapt}
    - ${plugins.tezov_project_config}
  lib_adr_app_core:
    - ${plugins.library}
    - ${plugins.kotlin}
    - ${plugins.kapt}
    - ${plugins.tezov_project_config}
  lib_adr_ui_cpt:
    - ${plugins.library}
    - ${plugins.kotlin}
    - ${plugins.kapt}
    - ${plugins.tezov_project_config}
  lib_adr_ui_core:
    - ${plugins.library}
    - ${plugins.kotlin}
    - ${plugins.kapt}
    - ${plugins.tezov_project_config}
  lib_adr_core:
    - ${plugins.library}
    - ${plugins.kotlin}
    - ${plugins.kapt}
    - ${plugins.tezov_project_config}
  lib_kmm_core:
    - ${plugins.library}
    - ${plugins.kotlin}
    - ${plugins.kapt}
    - ${plugins.tezov_project_config}
